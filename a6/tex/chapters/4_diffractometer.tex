\section{The Diffractometer}

\begin{figure}
	\centering
	\begin{minipage}[t]{0.6\linewidth}
		\centering
		\includegraphics[width=\textwidth]{../assets/xrd_setup.png}
		\captionof{figure}{XRD instrument setup. Image taken from \autocite{harrington_back--basics_2021}.}
		\label{fig:xrd_setup}
	\end{minipage}
	\hfill
	\begin{minipage}[t]{0.3\linewidth}
		\centering
		\includegraphics[width=\textwidth]{../assets/source.png}
		\captionof{figure}{Internal design of the x-ray tube. Image taken from \autocite{harrington_back--basics_2021}.}
		\label{fig:xray_tube}
	\end{minipage}
\end{figure}

A typical \ac{xrd} setup is depicted in \cref{fig:xrd_setup}. An x-ray source
with a tungsten cathode and a copper anode generates incident
$\ce{Cu}_{\mathrm{K}_{\alpha}}$ x-rays with a wavelength of
$\lambda=\qty{1.540592(2)}{\angstrom}$. The beam passes through the \ac{xrd}
incident optics and hits the sample at an angle $\omega$. The sample is glued onto
the sample holder of a goniometer. This device can rotate the sample through the
three Euler angles $\omega$, $\varphi$ and $\chi$. The scattered x-ray beam
passes through the receiving optics and is detected by an x-ray detector that is
mounted on a rocking cantilever. It forms an angle of $2\theta$ with the incident beam.
The instrument is contained in a radiation enclosure and is controlled by a computer.
The description of the individual instruments is based on \citeauthoryear{harrington_back--basics_2021}.

\subsection{X-Ray Source}

X-rays are produced in an evacuated x-ray tube, depicted in \cref{fig:xray_tube}.
Electrons are generated by thermionic emission from a heated tungsten cathode. As a
voltage of \qtyrange{10}{20}{\kilo\volt} is applied between the heated cathode
and a copper anode, the electrons are accelerated towards the anode. They
collide with the anode and ionize the copper atoms. During subsequent electronic relaxation,
a photon is emitted. Its energy depends on the available energy levels of the material.
The strongest radiation originates from the $K$-lines ($K_{\alpha 1}, K_{\alpha2}
, K_{\beta}$). $K_{\alpha 1}$ radiation is used for measurement, while $K_{\alpha
2}$ and $K_{\beta}$ are undesirable and therefore often filtered using a monochromator.
Excessive use of a tungsten cathode leads to tungsten contamination on the anode.
This results in additional peaks in the spectrum.

X-rays are focused on a strip and are passed through a beryllium window as either
point or line spots. The tube needs to be water-cooled due to the heat generated
by thermionic emission. It features a safety shutter which enables manual work on
the beam path or the sample without shutting down the tube.

\subsection{X-Ray Detector}

An x-ray detector converts x-rays into an electrical signal, usually measured in counts
per second (cps). There exists a variety of types, depending on the underlying physics,
for example: proportional counters, scintillation counters and solid-state
detectors.

Detectors can measure a pointlike region, a line or an area. As a consequence, a finite
window in reciprocal space is sampled during measurement. An important metric is
the maximum count rate. If this limit is exceeded, the detector starts
responding non-linearly or even sustains damage.

\subsection{Incident and Receiving Optics}

\begin{figure}
	\centering
	\begin{minipage}[t]{0.45\linewidth}
		\centering
		\includegraphics[width=\textwidth]{../assets/optics.png}
		\captionof{figure}{Incident and receiving optics. Image taken from \autocite{harrington_back--basics_2021}.}
		\label{fig:optics}
	\end{minipage}
	\hfill
	\begin{minipage}[t]{0.45\linewidth}
		\centering
		\includegraphics[width=\textwidth]{../assets/goniometer.png}
		\captionof{figure}{Goniometer geometry. Image taken from \autocite{harrington_back--basics_2021}.}
		\label{fig:goniometer}
	\end{minipage}
\end{figure}

A major challenge of the diffraction setup is to condition the divergent x-rays
that leave the source into a parallel beam before it reaches the sample. This conditioning
occurs within the incident beam optics, see \cref{fig:optics}. The procedure is necessary to avoid
divergence errors, as small deviations of the incoming radiation direction smear out
the scattering vector. A suitable conditioning device is a laterally graded
multilayer mirror. This mirror is designed for a specific wavelength, thus
reducing the unwanted $K_{\beta}$ radiation as well as Bremsstrahlung. It does not
reduce the x-ray intensity significantly. Since the divergence in the axial
direction is unaffected by the mirror, it must be constrained by Soller slits.
Installing an axial slit reduces the beam spot size.

To restrict the scattered radiation wavelength around the $K_{\alpha 1}$ peak, a channel-cut
crystal monochromator can be used in the receiving optics. The device consists
of two high-quality single-crystal germanium mirrors and is defined by the
crystal being used, the Miller index of the diffracting lattice plane, as well
as the number of reflections. While the laboratory instrument features a 2xGe(220)
monochromator, installing one leads to intensity loss. It is not required in
solid-state detector setups as they can select a narrow energy window.

Also installed in the receiving optics are attenuators, which are devices to reduce
the intensity of an x-ray beam. These are vital components for high-intensity
reflections that would otherwise damage the detector. A thin metal foil placed in
the beam path can be used as an attenuator.

\subsection{Goniometer}

The goniometer, depicted in \cref{fig:goniometer}, can adjust the different
angles between the x-ray source, sample and x-ray detector. Diffractometers used for
thin-film characterization usually feature a four-axis goniometer. In this setup,
the source is installed in a fixed position, while the sample stage can be rotated
through the angles $\varphi$ (inner axis), $\chi$ (intermediate axis) and
$\omega$ (outer axis). Additionally, the position of the detector can be
set using the angle $2\theta$. Most goniometers can translate the sample stage in
multiple directions.

\subsection{Coordinate Systems}
\label{sec:coordinate_systems}

An XRD experiment features two coordinate systems, see \cref{fig:coordinate_system_1}:
the two-dimensional laboratory frame (red), defined by the goniometer axes and the
reciprocal lattice (blue), which is defined by the crystal structure.

\begin{figure}
	\centering
	\newlength{\coordSysWidth}
	\settowidth{\coordSysWidth}{\includegraphics{
		../assets/coordinate_system_1.pdf
	}}
	\begin{minipage}[t]{\coordSysWidth}
		\centering
		\includegraphics{../assets/coordinate_system_1.pdf}
		\captionof{figure}{Laboratory frame and reciprocal lattice.}
		\label{fig:coordinate_system_1}
	\end{minipage}
	\hfill
	\begin{minipage}[t]{\coordSysWidth}
		\centering
		\includegraphics{../assets/coordinate_system_3.pdf}
		\captionof{figure}{Definition of $\omega$ and $2\theta$ angles.}
		\label{fig:coordinate_system_2}
	\end{minipage}
	\hfill
	\begin{minipage}[t]{\coordSysWidth}
		\centering
		\includegraphics{../assets/coordinate_system_2.pdf}
		\captionof{figure}{Definition of $\varphi$ and $\chi$ angles.}
		\label{fig:coordinate_system_3}
	\end{minipage}
	\label{fig:coordinate_systems}
\end{figure}

The laboratory frame coincides with the incidence plane, which is spanned by
$\mathbf{k}_{\mathrm{in}}$ and $\mathbf{k}_{\mathrm{out}}$. The frame is fixed by
the goniometer and does not depend on the crystal. Its two basis vectors are
defined as: $\hat{q}_{\parallel}$ -- the vector lies in the incidence plane and in
the sample holder surface. $\hat{q}_{\perp}$ -- the vector lies in the incidence
plane and is perpendicular to $\hat{q}_{\parallel}$. The scattering vector's coordinates
along these two axes are denoted $q_{\parallel}$ and $q_{\perp}$.

The operations of the goniometer either change the relative position of both coordinate
systems ($\varphi$ and $\chi$ rotation) or the scattering vector $\mathbf{q}$ ($\omega$
and $2\theta$ rotation).

Two angles $\theta$ and $\omega$ are used to describe $\mathbf{q}$ within the laboratory
frame and can be set using the goniometer, see \cref{fig:coordinate_system_2}. $2
\theta$ is the angle between $\mathbf{k}_{\mathrm{in}}$ and $\mathbf{k}_{\mathrm{out}}$
and $\omega$ is the angle between the sample holder surface and $\mathbf{k}_{\mathrm{in}}$.
The magnitude of $\mathbf{q}$ can be represented using only $\theta$ as:
\begin{align}
	\lvert \mathbf{q}\rvert^{2} & =\lvert \mathbf{k}_{\mathrm{out}}- \mathbf{k}_{\mathrm{in}}\rvert^{2}=k^{2}+k^{2}-2k^{2}\cos(2\theta)=2k^{2}(1-\cos(2\theta))=4k^{2}\sin^{2}(\theta) \\
	\lvert \mathbf{q}\rvert     & =2k \sin(\theta).
\end{align}
This reproduces the chord length of the Ewald sphere construction shown in \cref{fig:ewald_sphere},
which could previously only be expressed in terms of $\mathbf{k}_{\mathrm{in}}$
and $\mathbf{k}_{\mathrm{out}}$, since the diffractometer angles $\theta$ and $\omega$
had not yet been introduced.

To represent $\mathbf{k}_{\mathrm{in}}$ and $\mathbf{k}_{\mathrm{out}}$ using $\theta$
and $\omega$, note that $\omega$ is the negative polar angle of
$\mathbf{k}_{\mathrm{in}}$, and $\mathbf{k}_{\mathrm{out}}$ is obtained by rotating
$\mathbf{k}_{\mathrm{in}}$ by $2\theta$:
\begin{align}
	\mathbf{k}_{\mathrm{in}}  & = k \begin{pmatrix}\cos(\omega) \\ -\sin(\omega)\end{pmatrix} \nonumber                                                                                                                                                                                                                  \\
	\mathbf{k}_{\mathrm{out}} & = k \begin{pmatrix}\cos(2 \theta) & -\sin(2\theta) \\ +\sin(2\theta) & \cos(2\theta)\end{pmatrix} \begin{pmatrix}\cos(\omega) \\ -\sin(\omega)\end{pmatrix} =k \begin{pmatrix}\cos(2\theta-\omega) \\ \sin(2\theta-\omega)\end{pmatrix} \nonumber                                        \\
	\mathbf{q}                & =\mathbf{k}_{\mathrm{out}}-\mathbf{k}_{\mathrm{in}}=k \begin{pmatrix}\cos(2\theta-\omega)-\cos(\omega) \\ \sin(2\theta-\omega) + \sin(\omega)\end{pmatrix} = 2k \sin(\theta) \begin{pmatrix}\sin(\omega-\theta) \\ \cos(\omega-\theta)\end{pmatrix}. \label{eq:coordinate_transformation}
\end{align}
This equation also implies $q_{\parallel}/ q_{\perp}=\tan(\omega-\theta)$.

Both angles act on the system in different ways. Changing $2\theta$ only rotates the
detector, while leaving the orientation of the laboratory and crystallographic
frames unchanged. Changing $\omega$ rotates the laboratory frame as well as the reciprocal
lattice by the same amount, so that the reciprocal lattice does not move
relative to the laboratory frame. Because the laboratory frame itself has rotated,
the coordinates of the scattering vector change accordingly, as captured by \cref{eq:coordinate_transformation}.

The reciprocal lattice orientation depends on the direct lattice, and therefore
on the position of the crystal with respect to the sample holder. As $\theta$ only
moves the detector and $\omega$ moves both the laboratory frame and the reciprocal
lattice, neither angle changes the relative position between the two coordinate
systems. The two angles that operate on the reciprocal lattice, $\chi$ and
$\varphi$, share an important property: neither of them rotates the laboratory
frame. Instead, they rotate only the reciprocal lattice, which changes its orientation
relative to the laboratory frame, as depicted in \cref{fig:coordinate_system_3}.